// je veux un router de partage de post
import express from 'express';
import PartageController from '../controllers/PartageController.js';

const router = express.Router();
app.get('/post', PartageController);